import { Routes } from '@angular/router';
import { InicioComponent } from './inicio'; // Importamos lo que acabas de crear

export const routes: Routes = [
  { path: '', component: InicioComponent }, // Ruta vacía = Tu Index
];